# ============================================================================
# Shop Microservices - Environment Variables
# ============================================================================
# Copie este arquivo para .env e ajuste os valores conforme necessário.
# O arquivo .env NÃO deve ser commitado no repositório.
#
# cp .env.example .env
# ============================================================================

# ----------------------------------------------------------------------------
# SQL Server (Catalog.API)
# ----------------------------------------------------------------------------
# Senha do usuário SA do SQL Server
# IMPORTANTE: Use uma senha forte em produção!
# Requisitos: Mínimo 8 caracteres, com maiúsculas, minúsculas, números e símbolos
SQL_SA_PASSWORD=YourStrong@Passw0rd123

# ----------------------------------------------------------------------------
# MongoDB (Ordering.API)
# ----------------------------------------------------------------------------
# Senha do usuário root do MongoDB
# O usuário é fixo como 'root' no docker-compose
MONGO_PASSWORD=example

# ----------------------------------------------------------------------------
# RabbitMQ (Message Broker)
# ----------------------------------------------------------------------------
# Credenciais do RabbitMQ
# Acesse o Management UI em http://localhost:15672
RABBITMQ_USER=guest
RABBITMQ_PASSWORD=guest

# ----------------------------------------------------------------------------
# Seq (Centralized Logging)
# ----------------------------------------------------------------------------
# Seq não requer credenciais por padrão em desenvolvimento
# Para produção, configure API keys no painel de administração
# Acesse o Seq UI em http://localhost:5341

# ----------------------------------------------------------------------------
# ASP.NET Core
# ----------------------------------------------------------------------------
# Ambiente de execução (Development, Staging, Production)
ASPNETCORE_ENVIRONMENT=Development

# ----------------------------------------------------------------------------
# PORTAS DOS SERVIÇOS (valores padrão)
# ----------------------------------------------------------------------------
# Descomente e altere se houver conflito com outras aplicações
# Nota: Alterar essas portas requer modificar o docker-compose.yml

# Infrastructure
# SQL_SERVER_PORT=1433
# REDIS_PORT=6379
# MONGODB_PORT=27017
# RABBITMQ_PORT=5672
# RABBITMQ_MANAGEMENT_PORT=15672
# SEQ_PORT=5341

# Microservices
# CATALOG_API_PORT=5001
# BASKET_API_PORT=5002
# ORDERING_API_PORT=5003

# ----------------------------------------------------------------------------
# URLS DE ACESSO (após docker-compose up)
# ----------------------------------------------------------------------------
# Catalog API (Swagger):     http://localhost:5001
# Basket API (Swagger):      http://localhost:5002
# Ordering API (Swagger):    http://localhost:5003
# Seq (Logs UI):             http://localhost:5341
# RabbitMQ (Management UI):  http://localhost:15672

# ----------------------------------------------------------------------------
# CONFIGURAÇÕES DE PRODUÇÃO (não usar em desenvolvimento)
# ----------------------------------------------------------------------------
# Para produção, substitua os valores acima por senhas fortes:
#
# SQL_SA_PASSWORD=<senha-muito-forte-32-caracteres>
# MONGO_PASSWORD=<senha-muito-forte-32-caracteres>
# RABBITMQ_USER=admin
# RABBITMQ_PASSWORD=<senha-muito-forte-32-caracteres>
#
# Considerações de segurança para produção:
# - Use secrets management (Azure Key Vault, AWS Secrets Manager, etc.)
# - Configure SSL/TLS para todas as conexões
# - Restrinja acesso de rede aos bancos de dados
# - Configure backups automáticos
# - Implemente rate limiting nas APIs
# - Configure autenticação JWT nas APIs

# ----------------------------------------------------------------------------
# COMANDOS ÚTEIS
# ----------------------------------------------------------------------------
# Iniciar todos os serviços:
#   docker-compose up -d --build
#
# Ver logs de todos os serviços:
#   docker-compose logs -f
#
# Ver logs de um serviço específico:
#   docker-compose logs -f catalog-api
#
# Parar todos os serviços:
#   docker-compose down
#
# Parar e remover volumes (APAGA DADOS!):
#   docker-compose down -v
#
# Verificar status dos containers:
#   docker-compose ps
#
# Reiniciar um serviço específico:
#   docker-compose restart catalog-api
